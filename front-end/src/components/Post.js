import React from 'react';
import mockData from "../mockdata.json";

function Post(props){
    return(
        <div>
            {/* first name, last name, age, blood type*/}
        </div>
    )
}

export default Post;